"use client";
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { forwardRef } from "react";
import { Column, Flex, Line, Text } from ".";
const Timeline = forwardRef(({ items, alignment = "left", size = "m", ...flex }, ref) => {
    // Helper to get color variable for state
    const getStateColor = (state) => {
        switch (state) {
            case "active": return "var(--brand-solid-strong)";
            case "success": return "var(--success-solid-strong)";
            case "danger": return "var(--danger-solid-strong)";
            default: return "var(--neutral-solid-strong)";
        }
    };
    const dotSize = size === "xs" ? "8" : size === "s" ? "24" : size === "m" ? "32" : size === "l" ? "40" : "48";
    const isHorizontal = flex.direction === "row";
    const renderDot = (item, state) => (_jsx(Flex, { center: true, radius: "full", solid: state === "active" ? "brand-strong" : state === "success" ? "success-strong" : state === "danger" ? "danger-strong" : undefined, background: state === "default" ? "neutral-weak" : undefined, border: state === "success" ? "success-strong" : state === "danger" ? "danger-strong" : state === "active" ? "brand-strong" : "neutral-strong", minHeight: dotSize, maxHeight: dotSize, minWidth: dotSize, maxWidth: dotSize, children: item.marker && (_jsx(Flex, { center: true, onSolid: state === "active" ? "brand-strong" : state === "success" ? "success-strong" : state === "danger" ? "danger-strong" : undefined, onBackground: state === "default" ? "neutral-weak" : undefined, textVariant: "label-default-m", children: item.marker })) }));
    const markerWidth = isHorizontal ? undefined : dotSize;
    return (_jsx(Column, { ...flex, children: items.map((item, index) => {
            const state = item.state || "default";
            const nextState = index < items.length - 1 ? (items[index + 1].state || "default") : state;
            const currentColor = getStateColor(state);
            const nextColor = getStateColor(nextState);
            const gradientToNext = isHorizontal
                ? `linear-gradient(to right, ${currentColor}, ${nextColor})`
                : `linear-gradient(to bottom, ${currentColor}, ${nextColor})`;
            if (isHorizontal) {
                return (_jsxs(Flex, { direction: "column", fillWidth: true, children: [_jsxs(Column, { fillWidth: true, horizontal: "center", vertical: "center", direction: "row", children: [index !== 0 && (_jsx(Line, { background: undefined, solid: state === "active" ? "brand-strong" : state === "success" ? "success-strong" : state === "danger" ? "danger-strong" : "neutral-strong" })), renderDot(item, state), index !== items.length - 1 && (_jsx(Line, { background: undefined, style: { background: gradientToNext } }))] }), _jsxs(Column, { fillWidth: true, paddingX: "20", paddingTop: "12", paddingBottom: "24", horizontal: index === 0 ? "start" : index === items.length - 1 ? "end" : "center", align: index === 0 ? "left" : index === items.length - 1 ? "right" : "center", gap: "2", children: [item.label && (_jsx(Text, { variant: "label-default-m", onBackground: state === "danger" ? "danger-weak" : undefined, children: item.label })), item.description && (_jsx(Text, { variant: "body-default-s", onBackground: state === "danger" ? "danger-weak" : "neutral-weak", children: item.description })), item.children] })] }, index));
            }
            const rowDirection = alignment === "right" ? "row-reverse" : undefined;
            const hasContent = item.description || item.children;
            return (_jsxs(Flex, { fillWidth: true, direction: rowDirection, children: [_jsxs(Column, { horizontal: "center", minWidth: markerWidth, maxWidth: markerWidth, children: [index !== 0 && (_jsx(Line, { vert: true, fillHeight: true, background: undefined, solid: state === "active" ? "brand-strong" : state === "success" ? "success-strong" : state === "danger" ? "danger-strong" : "neutral-strong", minHeight: "8" })), renderDot(item, state), index !== items.length - 1 && (_jsx(Line, { vert: true, fillHeight: true, background: undefined, style: { background: gradientToNext }, minHeight: "8" }))] }), _jsxs(Column, { fillWidth: true, paddingX: "20", paddingBottom: index !== items.length - 1 ? "24" : undefined, vertical: "center", gap: "2", align: alignment === "right" ? "right" : undefined, children: [item.label && (_jsx(Text, { variant: "label-default-m", onBackground: state === "danger" ? "danger-weak" : undefined, children: item.label })), item.description && (_jsx(Text, { variant: "body-default-s", onBackground: state === "danger" ? "danger-weak" : "neutral-weak", children: item.description })), item.children] })] }, index));
        }) }));
});
Timeline.displayName = "Timeline";
export { Timeline };
//# sourceMappingURL=Timeline.js.map