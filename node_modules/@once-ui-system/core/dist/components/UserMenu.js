"use client";
import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef } from "react";
import classNames from "clsx";
import { DropdownWrapper, User, Column } from ".";
import styles from "./UserMenu.module.scss";
const UserMenu = forwardRef(({ selected = false, dropdown, minWidth, maxWidth, minHeight, placement, className, style, loading, ...userProps }, ref) => {
    return (_jsx(DropdownWrapper, { minWidth: minWidth, maxWidth: maxWidth, minHeight: minHeight, placement: placement, disableTriggerClick: loading, style: {
            borderRadius: "var(--radius-full)",
        }, trigger: _jsx(Column, { tabIndex: loading ? -1 : 0, padding: "4", radius: "full", cursor: loading ? "default" : "interactive", border: selected ? "neutral-medium" : "transparent", background: selected ? "neutral-strong" : "transparent", pointerEvents: loading ? "none" : "auto", className: classNames(className || "", selected ? styles.selected : "", styles.wrapper), style: style, children: _jsx(User, { loading: loading, ...userProps }) }), dropdown: dropdown }));
});
UserMenu.displayName = "UserMenu";
export { UserMenu };
//# sourceMappingURL=UserMenu.js.map