"use client";
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useState, useEffect, forwardRef, useCallback, } from "react";
import classNames from "clsx";
import { Column, Row, Text, Spinner } from ".";
import styles from "./Input.module.scss";
import { useDebounce } from "../hooks/useDebounce";
const Input = forwardRef(({ id, label, placeholder, height = "m", error = false, errorMessage, description, radius, className, style, hasPrefix, hasSuffix, variant = "default", characterCount, loading = false, children, onFocus, onBlur, validate, cursor, ...props }, ref) => {
    const [isFocused, setIsFocused] = useState(false);
    const [isFilled, setIsFilled] = useState(!!props.value);
    const [validationError, setValidationError] = useState(null);
    const debouncedValue = useDebounce(props.value, 1000);
    const handleFocus = (event) => {
        setIsFocused(true);
        if (onFocus)
            onFocus(event);
    };
    const handleBlur = (event) => {
        setIsFocused(false);
        if (event.target.value) {
            setIsFilled(true);
        }
        else {
            setIsFilled(false);
        }
        if (onBlur)
            onBlur(event);
    };
    useEffect(() => {
        setIsFilled(!!props.value);
    }, [props.value]);
    const validateInput = useCallback(() => {
        if (!debouncedValue) {
            setValidationError(null);
            return;
        }
        if (validate) {
            const error = validate(debouncedValue);
            if (error) {
                setValidationError(error);
            }
            else {
                setValidationError(errorMessage || null);
            }
        }
        else {
            setValidationError(null);
        }
    }, [debouncedValue, validate, errorMessage]);
    useEffect(() => {
        validateInput();
    }, [debouncedValue, validateInput]);
    const displayError = validationError || errorMessage;
    const fontSizeMap = {
        xs: "font-s",
        s: "font-s",
        m: "font-m",
        l: "font-l",
        xl: "font-xl",
    };
    const inputClassNames = classNames(styles.input, "font-body", "font-default", fontSizeMap[height], cursor === "interactive" ? "cursor-interactive" : undefined, {
        [styles.filled]: isFilled,
        [styles.focused]: isFocused,
        [styles.withPrefix]: hasPrefix,
        [styles.withSuffix]: hasSuffix,
        [styles.placeholder]: placeholder,
        [styles.hasChildren]: children,
        [styles.error]: displayError && debouncedValue !== "",
    });
    return (_jsxs(Column, { gap: "8", style: style, fillWidth: true, fitHeight: true, className: classNames(className, {
            [styles.error]: (error || (displayError && debouncedValue !== "")) && props.value !== "",
        }), children: [_jsxs(Row, { transition: "micro-medium", border: variant === "ghost" ? "transparent" : "neutral-medium", background: variant === "ghost" ? "transparent" : "neutral-alpha-weak", overflow: "hidden", vertical: "stretch", className: classNames(styles.base, height && styles[height], radius === "none" ? "radius-none" : radius ? `radius-l-${radius}` : "radius-l"), children: [hasPrefix && (_jsx(Row, { paddingLeft: "12", className: styles.prefix, position: "static", children: hasPrefix })), _jsxs(Column, { fillWidth: true, padding: "4", children: [_jsx("input", { ...props, ref: ref, id: id, placeholder: placeholder, onFocus: handleFocus, onBlur: handleBlur, className: inputClassNames, "aria-describedby": displayError ? `${id}-error` : undefined, "aria-invalid": !!displayError }), label && (_jsx(Text, { as: "label", variant: "label-default-m", htmlFor: id, className: classNames(styles.label, styles.inputLabel, {
                                    [styles.floating]: isFocused || isFilled || placeholder,
                                }), children: label })), children] }), characterCount && props.maxLength && (_jsx(Row, { paddingRight: "12", className: styles.suffix, position: "static", children: _jsx(Text, { variant: "label-default-s", onBackground: props.maxLength - String(props.value || '').length <= 5
                                ? "danger-weak"
                                : props.maxLength - String(props.value || '').length <= 10
                                    ? "warning-weak"
                                    : "neutral-weak", children: props.maxLength - String(props.value || '').length }) })), loading && (_jsx(Row, { paddingRight: "12", className: styles.suffix, position: "static", children: _jsx(Spinner, { size: "s" }) })), hasSuffix && !loading && (_jsx(Row, { paddingRight: "12", className: styles.suffix, position: "static", children: hasSuffix }))] }), displayError && errorMessage !== false && (_jsx(Row, { paddingX: "16", id: `${id}-error`, textVariant: "body-default-s", onBackground: "danger-weak", children: validationError || errorMessage })), description && (_jsx(Row, { paddingX: "16", id: `${id}-description`, textVariant: "body-default-s", onBackground: "neutral-weak", children: description }))] }));
});
Input.displayName = "Input";
export { Input };
//# sourceMappingURL=Input.js.map