"use client";
import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef } from "react";
import { ElementType } from "./ElementType";
import { Flex, Icon, Tooltip, HoverCard, Spinner } from ".";
import buttonStyles from "./Button.module.scss";
import iconStyles from "./IconButton.module.scss";
import classNames from "clsx";
const IconButton = forwardRef(({ icon = "refresh", size = "m", id, radius, rounded = false, tooltip, tooltipPosition = "top", variant = "primary", loading = false, disabled = false, href, children, color, className, style, ...props }, ref) => {
    const radiusSize = size === "s" || size === "m" ? "m" : "l";
    const button = (_jsx(ElementType, { id: id, href: href, ref: ref, disabled: disabled, "data-disabled": disabled ? true : undefined, "data-border": rounded ? "rounded" : undefined, className: classNames(buttonStyles.button, buttonStyles[variant], iconStyles[size], radius === "none"
            ? "radius-none"
            : radius
                ? `radius-${radiusSize}-${radius}`
                : `radius-${radiusSize}`, "text-decoration-none", "button", disabled ? "cursor-not-allowed" : "cursor-interactive", className), style: style, "aria-label": (tooltip || icon), "aria-disabled": disabled, ...props, children: _jsx(Flex, { fill: true, center: true, children: loading ? (_jsx(Spinner, { size: size === "l" ? "s" : "xs" })) : children ? (children) : (_jsx(Icon, { name: icon, size: "s", onBackground: color })) }) }));
    if (tooltip) {
        return (_jsx(HoverCard, { trigger: button, placement: tooltipPosition, fade: 0, scale: 0.9, duration: 200, offsetDistance: "4", children: _jsx(Tooltip, { label: tooltip }) }));
    }
    return button;
});
IconButton.displayName = "IconButton";
export { IconButton };
//# sourceMappingURL=IconButton.js.map