"use client";
import { jsx as _jsx } from "react/jsx-runtime";
import { useState, useEffect, useRef, forwardRef } from "react";
import { ToggleButton, Scroller, Flex } from ".";
const SegmentedControl = forwardRef(({ buttons, onToggle, defaultSelected, fillWidth = true, selected, compact = false, className, style, ...scrollerProps }, ref) => {
    const [internalSelected, setInternalSelected] = useState(() => {
        if (selected !== undefined)
            return selected;
        if (defaultSelected !== undefined)
            return defaultSelected;
        return buttons[0]?.value || "";
    });
    const buttonRefs = useRef([]);
    useEffect(() => {
        if (selected !== undefined) {
            setInternalSelected(selected);
        }
    }, [selected]);
    const handleButtonClick = (clickedButton, event) => {
        event.stopPropagation();
        const newSelected = clickedButton.value;
        setInternalSelected(newSelected);
        onToggle(newSelected, event);
    };
    const handleKeyDown = (event) => {
        const focusedIndex = buttonRefs.current.findIndex((ref) => ref === document.activeElement);
        switch (event.key) {
            case "ArrowLeft":
            case "ArrowUp":
                event.preventDefault();
                const prevIndex = focusedIndex === -1
                    ? buttons.length - 1 // If nothing is focused, focus the last item
                    : focusedIndex > 0
                        ? focusedIndex - 1
                        : buttons.length - 1;
                buttonRefs.current[prevIndex]?.focus();
                break;
            case "ArrowRight":
            case "ArrowDown":
                event.preventDefault();
                const nextIndex = focusedIndex === -1
                    ? 0 // If nothing is focused, focus the first item
                    : focusedIndex < buttons.length - 1
                        ? focusedIndex + 1
                        : 0;
                buttonRefs.current[nextIndex]?.focus();
                break;
            case "Enter":
            case " ": // Space key
                event.preventDefault();
                if (focusedIndex >= 0 && focusedIndex < buttons.length) {
                    const focusedButton = buttons[focusedIndex];
                    setInternalSelected(focusedButton.value);
                    onToggle(focusedButton.value);
                }
                break;
            default:
                return;
        }
    };
    const selectedIndex = buttons.findIndex((button) => button.value === internalSelected);
    return (_jsx(Scroller, { direction: "row", fillWidth: fillWidth, minWidth: 0, ...scrollerProps, role: "tablist", "aria-orientation": "horizontal", onKeyDown: handleKeyDown, children: _jsx(Flex, { fillWidth: fillWidth, gap: compact ? "-1" : "2", padding: compact ? "0" : "4", radius: "l", border: compact ? undefined : "neutral-alpha-weak", children: buttons.map((button, index) => {
                return (_jsx(ToggleButton, { ref: (el) => {
                        buttonRefs.current[index] = el;
                    }, variant: compact ? "outline" : "ghost", radius: compact ? (index === 0 ? "left" : index === buttons.length - 1 ? "right" : "none") : undefined, selected: index === selectedIndex, onClick: (event) => handleButtonClick(button, event), role: "tab", className: className, style: { opacity: (index !== selectedIndex && !compact) ? 0.6 : 1, ...style }, "aria-selected": index === selectedIndex, "aria-controls": `panel-${button.value}`, tabIndex: index === selectedIndex ? 0 : -1, fillWidth: fillWidth, ...button }, button.value));
            }) }) }));
});
SegmentedControl.displayName = "SegmentedControl";
export { SegmentedControl };
//# sourceMappingURL=SegmentedControl.js.map