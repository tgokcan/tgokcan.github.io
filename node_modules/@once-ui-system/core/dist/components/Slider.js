"use client";
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { forwardRef, useRef, useState, useEffect } from "react";
import classNames from "clsx";
import { Column, Row, Text } from ".";
import styles from "./Slider.module.scss";
const Slider = forwardRef(({ value, onChange, min = 0, max = 100, step = 1, label, showValue = false, disabled = false, className, style, ...props }, ref) => {
    const sliderRef = useRef(null);
    const [isDragging, setIsDragging] = useState(false);
    const percentage = ((value - min) / (max - min)) * 100;
    const handleChange = (e) => {
        const newValue = parseFloat(e.target.value);
        onChange(newValue);
    };
    useEffect(() => {
        if (isDragging) {
            const handleMouseUp = () => setIsDragging(false);
            window.addEventListener("mouseup", handleMouseUp);
            return () => window.removeEventListener("mouseup", handleMouseUp);
        }
    }, [isDragging]);
    return (_jsxs(Column, { fillWidth: true, gap: "8", className: classNames(styles.container, className), style: style, children: [(label || showValue) && (_jsxs(Row, { fillWidth: true, horizontal: "between", vertical: "center", children: [label && (_jsx(Text, { variant: "label-default-s", onBackground: "neutral-weak", children: label })), showValue && (_jsx(Text, { variant: "label-default-s", onBackground: "neutral-medium", children: value }))] })), _jsxs(Row, { fillWidth: true, height: "40", className: classNames(styles.sliderWrapper, {
                    [styles.disabled]: disabled,
                    [styles.dragging]: isDragging,
                }), children: [_jsx("div", { className: styles.track, children: _jsx("div", { className: styles.fill, style: { width: `${percentage}%` } }) }), _jsx("input", { ref: ref || sliderRef, type: "range", min: min, max: max, step: step, value: value, onChange: handleChange, onMouseDown: () => setIsDragging(true), disabled: disabled, className: styles.input, ...props }), _jsx("div", { className: styles.thumb, style: { left: `${percentage}%` } })] })] }));
});
Slider.displayName = "Slider";
export { Slider };
//# sourceMappingURL=Slider.js.map