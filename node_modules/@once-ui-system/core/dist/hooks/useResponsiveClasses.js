"use client";
import { useCallback, useEffect, useRef } from "react";
// Helper function to get all possible class names for a property
const getPropertyClassNames = (property, baseDirection) => {
    const classNames = [];
    switch (property) {
        case "position":
            classNames.push("position-static", "position-relative", "position-absolute", "position-fixed", "position-sticky");
            break;
        case "direction":
            classNames.push("flex-row", "flex-row-reverse", "flex-column", "flex-column-reverse");
            break;
        case "horizontal":
            !baseDirection || baseDirection === "row" || baseDirection === "row-reverse"
                ? classNames.push("justify-start", "justify-end", "justify-center", "justify-between", "justify-around", "justify-evenly")
                : classNames.push("align-start", "align-end", "align-center", "align-stretch");
            break;
        case "vertical":
            !baseDirection || baseDirection === "row" || baseDirection === "row-reverse"
                ? classNames.push("align-start", "align-end", "align-center", "align-stretch")
                : classNames.push("justify-start", "justify-end", "justify-center", "justify-between", "justify-around", "justify-evenly");
            break;
        case "overflow":
            classNames.push("overflow-visible", "overflow-hidden", "overflow-scroll", "overflow-auto");
            break;
        case "overflowX":
            classNames.push("overflow-x-visible", "overflow-x-hidden", "overflow-x-scroll", "overflow-x-auto");
            break;
        case "overflowY":
            classNames.push("overflow-y-visible", "overflow-y-hidden", "overflow-y-scroll", "overflow-y-auto");
            break;
        case "columns":
            // Generate common column numbers
            for (let i = 1; i <= 12; i++) {
                classNames.push(`columns-${i}`);
            }
            break;
        case "flex":
            // Generate common flex classes
            for (let i = 1; i <= 12; i++) {
                classNames.push(`flex-${i}`);
            }
            break;
        case "wrap":
            classNames.push("flex-wrap", "flex-nowrap", "flex-wrap-reverse");
            break;
        case "hide":
            classNames.push("flex-hide", "flex-show");
            break;
        case "align":
            classNames.push("align-start", "align-center", "align-end");
            break;
        case "scrollbar":
            classNames.push("scrollbar", "scrollbar-hide", "scrollbar-show");
            break;
        case "pointerEvents":
            classNames.push("pointer-events-none", "pointer-events-auto");
            break;
        case "opacity":
            classNames.push("opacity-0", "opacity-10", "opacity-20", "opacity-30", "opacity-40", "opacity-50", "opacity-60", "opacity-70", "opacity-80", "opacity-90", "opacity-100");
            break;
        case "zIndex":
            classNames.push("z-index-1", "z-index-10", "z-index-100", "z-index-1000", "z-index-10000");
            break;
        case "transition":
            classNames.push("transition-all", "transition-colors", "transition-transform");
            break;
        case "top":
            classNames.push("top-0", "top-1", "top-2", "top-4", "top-8", "top-12", "top-16", "top-20", "top-24", "top-32", "top-40", "top-48", "top-56", "top-64", "top-80", "top-104", "top-128", "top-160");
            break;
        case "right":
            classNames.push("right-0", "right-1", "right-2", "right-4", "right-8", "right-12", "right-16", "right-20", "right-24", "right-32", "right-40", "right-48", "right-56", "right-64", "right-80", "right-104", "right-128", "right-160");
            break;
        case "bottom":
            classNames.push("bottom-0", "bottom-1", "bottom-2", "bottom-4", "bottom-8", "bottom-12", "bottom-16", "bottom-20", "bottom-24", "bottom-32", "bottom-40", "bottom-48", "bottom-56", "bottom-64", "bottom-80", "bottom-104", "bottom-128", "bottom-160");
            break;
        case "left":
            classNames.push("left-0", "left-1", "left-2", "left-4", "left-8", "left-12", "left-16", "left-20", "left-24", "left-32", "left-40", "left-48", "left-56", "left-64", "left-80", "left-104", "left-128", "left-160");
            break;
    }
    return classNames;
};
export const useResponsiveClasses = (elementRef, baseDirection, responsiveProps, currentBreakpoint) => {
    const appliedClasses = useRef(new Set());
    const originalClasses = useRef([]);
    const isInitialized = useRef(false);
    const applyResponsiveClasses = useCallback(() => {
        if (!elementRef || !elementRef.current)
            return;
        const element = elementRef.current;
        // Store original classes on first run
        if (!isInitialized.current) {
            originalClasses.current = Array.from(element.classList);
            isInitialized.current = true;
        }
        // Remove all previously applied responsive classes
        appliedClasses.current.forEach((className) => {
            element.classList.remove(className);
        });
        appliedClasses.current.clear();
        // Helper function to get value with cascading fallback
        const getValueWithCascading = (property) => {
            const { xl, l, m, s, xs } = responsiveProps;
            switch (currentBreakpoint) {
                case "xl":
                    return xl?.[property];
                case "l":
                    return l?.[property] !== undefined ? l[property] : xl?.[property];
                case "m":
                    return m?.[property] !== undefined
                        ? m[property]
                        : l?.[property] !== undefined
                            ? l[property]
                            : xl?.[property];
                case "s":
                    return s?.[property] !== undefined
                        ? s[property]
                        : m?.[property] !== undefined
                            ? m[property]
                            : l?.[property] !== undefined
                                ? l[property]
                                : xl?.[property];
                case "xs":
                    return xs?.[property] !== undefined
                        ? xs[property]
                        : s?.[property] !== undefined
                            ? s[property]
                            : m?.[property] !== undefined
                                ? m[property]
                                : l?.[property] !== undefined
                                    ? l[property]
                                    : xl?.[property];
                default:
                    return undefined;
            }
        };
        // Properties to check for responsive classes
        const properties = [
            "position",
            "direction",
            "horizontal",
            "vertical",
            // Display properties
            "overflow",
            "overflowX",
            "overflowY",
            // Grid properties
            "columns",
            // Flex properties
            "flex",
            "wrap",
            "show",
            "hide",
            // Position offsets
            "top",
            "right",
            "bottom",
            "left",
            // Additional class-based properties
            "align",
            "scrollbar",
            "pointerEvents",
            "opacity",
            "zIndex",
            "transition",
        ];
        properties.forEach((property) => {
            const value = getValueWithCascading(property);
            if (value !== undefined) {
                // Remove all possible classes for this property
                const possibleClasses = getPropertyClassNames(property, baseDirection);
                possibleClasses.forEach((possibleClass) => {
                    if (element.classList.contains(possibleClass)) {
                        element.classList.remove(possibleClass);
                    }
                });
                let className = "";
                switch (property) {
                    case "position":
                        className = `position-${value}`;
                        break;
                    case "direction":
                        className = `flex-${value}`;
                        break;
                    case "horizontal":
                    case "vertical":
                        const direction = getValueWithCascading("direction") || baseDirection;
                        const isRowDirection = !direction || direction === "row" || direction === "row-reverse";
                        className =
                            property === "horizontal"
                                ? isRowDirection
                                    ? `justify-${value}`
                                    : `align-${value}`
                                : isRowDirection
                                    ? `align-${value}`
                                    : `justify-${value}`;
                        break;
                    // Display properties
                    case "overflow":
                        className = `overflow-${value}`;
                        break;
                    case "overflowX":
                        className = `overflow-x-${value}`;
                        break;
                    case "overflowY":
                        className = `overflow-y-${value}`;
                        break;
                    // Grid properties
                    case "columns":
                        className = `columns-${value}`;
                        break;
                    // Flex properties
                    case "flex":
                        className = `flex-${value}`;
                        break;
                    case "wrap":
                        className = `flex-${value}`;
                        break;
                    case "hide":
                        className = value ? "flex-hide" : "flex-show";
                        break;
                    // Additional class-based properties
                    case "align":
                        className = `align-${value}`;
                        break;
                    case "scrollbar":
                        className = `scrollbar-${value}`;
                        break;
                    case "pointerEvents":
                        className = `pointer-events-${value}`;
                        break;
                    case "opacity":
                        className = `opacity-${value}`;
                        break;
                    case "zIndex":
                        className = `z-index-${value}`;
                        break;
                    case "transition":
                        className = `transition-${value}`;
                        break;
                    // Position offsets
                    case "top":
                        className = `top-${value}`;
                        break;
                    case "right":
                        className = `right-${value}`;
                        break;
                    case "bottom":
                        className = `bottom-${value}`;
                        break;
                    case "left":
                        className = `left-${value}`;
                        break;
                }
                if (className) {
                    element.classList.add(className);
                    appliedClasses.current.add(className);
                }
            }
            else {
                // If value is undefined, restore original classes for this property
                const possibleClasses = getPropertyClassNames(property, baseDirection);
                const originalClassesToRestore = originalClasses.current.filter((originalClass) => possibleClasses.includes(originalClass));
                // First remove all possible classes for this property
                possibleClasses.forEach((possibleClass) => {
                    if (element.classList.contains(possibleClass)) {
                        element.classList.remove(possibleClass);
                    }
                });
                // Then restore original classes for this property
                originalClassesToRestore.forEach((originalClass) => {
                    if (!element.classList.contains(originalClass)) {
                        element.classList.add(originalClass);
                    }
                });
            }
        });
    }, [elementRef, responsiveProps, currentBreakpoint, baseDirection]);
    useEffect(() => {
        applyResponsiveClasses();
    }, [applyResponsiveClasses]);
    // Cleanup on unmount
    useEffect(() => {
        return () => {
            if (elementRef.current) {
                appliedClasses.current.forEach((className) => {
                    elementRef.current?.classList.remove(className);
                });
            }
        };
    }, []);
};
//# sourceMappingURL=useResponsiveClasses.js.map