import React from "react";
type NumericKeys<T> = Array<Extract<keyof T, string>>;
type StreamingTarget<T> = Partial<Record<Extract<keyof T, string>, number>>;
type StreamingViewportOptions<T extends Record<string, any>> = {
    initialData: T[];
    windowSize: number;
    intervalMs: number;
    stepMs?: number;
    xKey: Extract<keyof T, string>;
    numericKeys: NumericKeys<T>;
    getNextTarget: (ctx: {
        lastPoint: T;
        nextX: number;
    }) => StreamingTarget<T>;
};
type StreamingViewportResult<T> = {
    data: T[];
    xDomain: [number, number];
    running: boolean;
    setRunning: React.Dispatch<React.SetStateAction<boolean>>;
    reset: () => void;
};
export declare function useStreamingViewport<T extends Record<string, any>>(options: StreamingViewportOptions<T>): StreamingViewportResult<T>;
export {};
//# sourceMappingURL=useStreamingViewport.d.ts.map