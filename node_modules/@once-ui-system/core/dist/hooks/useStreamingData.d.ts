import React from "react";
type NumericKeys<T> = Array<Extract<keyof T, string>>;
type StreamingTarget<T> = Partial<Record<Extract<keyof T, string>, number>>;
type StreamingOptions<T extends Record<string, any>> = {
    initialData: T[];
    windowSize: number;
    intervalMs: number;
    stepMs?: number;
    xKey: Extract<keyof T, string>;
    numericKeys: NumericKeys<T>;
    getNextTarget: (ctx: {
        lastPoint: T;
        nextX: number;
    }) => StreamingTarget<T>;
};
type StreamingResult<T> = {
    data: T[];
    running: boolean;
    setRunning: React.Dispatch<React.SetStateAction<boolean>>;
    reset: () => void;
};
export declare function useStreamingData<T extends Record<string, any>>(options: StreamingOptions<T>): StreamingResult<T>;
export {};
//# sourceMappingURL=useStreamingData.d.ts.map